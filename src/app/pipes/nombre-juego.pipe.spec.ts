import { NombreJuegoPipe } from './nombre-juego.pipe';

describe('NombreJuegoPipe', () => {
  it('create an instance', () => {
    const pipe = new NombreJuegoPipe();
    expect(pipe).toBeTruthy();
  });
});
